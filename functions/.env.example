# ServicePower API Credentials
# Copy this file to .env and fill in your actual credentials
# NEVER commit the .env file to git!

# For local development only (not used in Firebase deployment)
SP_USER_ID=your_user_id_here
SP_PASSWORD=your_password_here
SP_SVCRACCT=your_svcracct_here
SP_MFG_ID=your_manufacturer_id_here  # Optional

# IMPORTANT: Use web service-specific credentials, NOT website login credentials!
# Manufacturer clients need separate web service credentials from ServicePower
# Service centers can use their website credentials

# For Firebase deployment, use Firebase Functions config instead:
# firebase functions:config:set servicepower.userid="your_user_id"
# firebase functions:config:set servicepower.password="your_password"
# firebase functions:config:set servicepower.svcracct="your_svcracct"
# firebase functions:config:set servicepower.mfgid="your_manufacturer_id"
